{
    "name": "football-matches-api",
    "version": "1.0.0",
    "description": "API backend for football matches",
    "main": "server.js",
    "scripts": {
        "start": "NODE_ENV=production node server",
        "dev": "nodemon server",
        "test": "mocha --reporter json > test-results.json --exit --recursive --timeout 10000 tests",
        "test_console": "mocha --exit --recursive --timeout 10000 tests"
    },
    "repository": {
        "type": "git",
        "url": "git@github.com:thulin82/football-matches-api.git"
    },
    "author": "Markus Thulin",
    "license": "MIT",
    "dependencies": {
        "chai": "^4.3.0",
        "chai-http": "^4.3.0",
        "cors": "^2.8.0",
        "dotenv": "^16.4.0",
        "express": "^4.21.0",
        "mocha": "^10.2.0",
        "mongoose": "^8.8.0",
        "morgan": "^1.10.0",
        "node-geocoder": "^4.4.0",
        "slugify": "^1.6.0"
    },
    "devDependencies": {
        "eslint": "^9.16.0",
        "nodemon": "^3.1.0"
    }
}
